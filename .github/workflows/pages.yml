# Publishes this folder to GitHub Pages on every push to main.
# Live URL: https://darkstone007.github.io/Journal/
name: Deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Fetch tree logo
        run: |
          mkdir -p assets
          curl -fsSL -o assets/logo.jpg https://raw.githubusercontent.com/Darkstone007/Portfolio/main/Media/Lo.jpg
      - uses: actions/configure-pages@v5
        with:
          enablement: true
      - uses: actions/upload-pages-artifact@v3
        with:
          path: .
      - id: deployment
        uses: actions/deploy-pages@v4

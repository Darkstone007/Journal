# Studio (owner only)

This subdirectory is the writing desk.

- It is **not** in the public navigation. Bookmark `studio/index.html`.
- Anyone can open the page. **Nobody can publish** without a GitHub token that belongs to **Darkstone007** and can write this repo.
- The token is stored in this browser only (`localStorage`) and is sent only to `api.github.com`.

Open the live studio after Pages is on:

https://darkstone007.github.io/Journal/studio/
